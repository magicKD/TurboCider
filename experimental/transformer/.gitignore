# Local binaries and generated model/weight caches
/build/
/cache/

# Generated by the adapter scripts; preserve baseline.mm and *.inc sources
/src/runner*.mm

# SVGs are the report figures; PNGs are redundant preview exports
/notes/figures/*.png

# Local experiment data; reproduce locally instead of committing
*.json
*.jsonl

# Smoke diagnostics
/notes/raw/*.stderr
