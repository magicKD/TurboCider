# TurboCider 推理核心

这里只包含正式推理库：core、backends、models、media。App、CLI、服务、SDK与测试分别位于顶层对应目录。

使用方法见 [使用文档](../docs/USAGE.md)，模块职责见 [实现状态](../docs/design/rewrite-implementation-status.md)，构建从仓库根目录执行 `make build`。H3/LTX未验收草稿位于 `experimental/video/`，不进入此库。
